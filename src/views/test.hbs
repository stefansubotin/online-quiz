<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>App</title>
    <script src="https://js.pusher.com/8.2.0/pusher.min.js"></script>
    <script>
      // Enable pusher logging - don't include this in production
      Pusher.logToConsole = true;

      var pusher = new Pusher('cefecd31795a4e419288', {
        cluster: 'eu'
      });

      var channel = pusher.subscribe('my-channel');
      channel.bind('my-event', function(data) {
        document.getElementById("divTest").innerHTML = data.message;
      });
    </script>
  </head>
  <body>
    <div>{{message}}</div><br>
    <div id="divTest">Hello</div><br><br>
    <input type="text" id="testInput" name="testInput"><br><br>
    <button onclick="onButtonTestInputClick()">Test</button>
    <button>Nada</button>
    <script>
      function onButtonTestInputClick(){
        onButtonClick("testInput");
      };

      function onButtonClick(string eleId){
        var url = window.location.href;
        var urlArray = url.split('/');
        var cod = 'my-channel';//urlArray[urlArray.length - 1];
        var mes = document.getElementById(eleId).value;
  	    fetch("https://filthy-sweatsuit-boa.cyclic.cloud/update", {
          method: "POST",
          body: JSON.stringify({
            id: eleId,
            code: cod,
            message: mes
          }),
          headers: {
            "Content-type": "application/json; charset=UTF-8"
          }
        })
      };
    </script>
  </body>
</html>